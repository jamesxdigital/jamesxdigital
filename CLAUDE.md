# CLAUDE.md

This is the GitHub profile README for `jamesxdigital` — shown publicly at github.com/jamesxdigital. It's a short personal bio/tagline page only — deliberately **not** a project index. A prior version listed every repo by category; that was removed (2026-07-06) because it went stale and cluttered the profile. Don't re-add a repo-listing section here.

## Committing

```bash
cd ~/Documents/GitHub/jamesxdigital && git add -A && git commit -m "Update: <what changed>" && git push
```
